# 단계별 개발 프롬프트

`PRD.md`를 토대로 한 4단계 프롬프트. 각 단계를 **새 대화 또는 순차적으로** 하나씩 붙여넣어 실행한다.
각 단계가 끝나면 직접 `streamlit run app.py`로 확인한 뒤 다음 단계로 넘어간다.

---

## 1단계: 기본 뼈대 작성

> **목표:** 실행되는 최소 앱. 아이템 추가와 목록 표시까지만 동작하면 성공.

```
같은 폴더의 PRD.md를 읽고, 1단계로 앱의 기본 뼈대만 작성해줘.

만들 파일:
1. app.py
2. requirements.txt

1단계 범위 (이것만 하고 멈춰):
- requirements.txt: streamlit>=1.30 한 줄
- st.set_page_config로 페이지 제목/아이콘 설정
- PRD 6장의 세션 상태 초기화 함수 init_state()
  - items: list[dict], 각 항목은 {"id": int, "name": str, "done": bool}
  - next_id: int (1부터 시작)
  - editing_id: int | None (초기값 None)
  - 이미 키가 있으면 덮어쓰지 말 것
- PRD 8.1의 함수들을 실제 동작하는 형태로 정의:
  add_item(name), delete_item(item_id), update_item(item_id, name),
  toggle_item(item_id), validate_name(name, exclude_id=None)
  → 이 단계에서는 검증 로직 없이 동작만 (validate_name은 일단 True 반환)
- 화면: 제목 → 입력창+추가 버튼 → 아이템 목록을 st.write로 단순 나열
- 각 아이템 행은 st.columns로 [체크박스 | 이름 | 수정 | 삭제] 4열 자리만 잡아두기
  (수정/삭제 버튼은 배치만 하고 동작은 2단계에서 붙임)

지켜야 할 규칙:
- 모든 위젯 key는 아이템 id를 포함할 것. 예: f"chk_{item['id']}"
  리스트 인덱스를 key에 쓰지 말 것 (삭제 시 상태가 밀림)
- 상태를 바꾼 뒤에는 st.rerun()으로 다시 그릴 것
- 파일은 app.py 하나로 유지. 위에서 아래로 읽히는 순서로 작성
- 아직 하지 말 것: 입력 검증, 편집 모드, 필터, 진행률 바, CSS/스타일링

완료 후 실행 방법 한 줄만 알려줘.
```

**확인 사항:** 앱이 뜬다 / 아이템을 추가하면 목록에 보인다 / 콘솔에 에러가 없다.

---

## 2단계: 기능 추가

> **목표:** PRD의 FR-1 ~ FR-5를 전부 동작시킨다. 디자인은 아직 신경 쓰지 않는다.

```
PRD.md의 5장 기능 요구사항(FR-1 ~ FR-5)을 app.py에 전부 구현해줘.
디자인/스타일링은 3단계에서 할 거니까 이번엔 동작에만 집중해.

FR-1 아이템 추가:
- Enter 키로도 추가 (st.form + clear_on_submit=True 사용)
- validate_name()에 실제 검증 채우기:
  · 앞뒤 공백 제거 후 빈 문자열이면 거부
  · 50자 초과 거부
  · 중복 거부 (대소문자 무시 + 공백 제거하고 비교, exclude_id는 비교에서 제외)
- 거부 시 st.warning으로 이유를 사람이 읽을 수 있게 표시
- 새 항목은 목록 맨 아래에 done=False로 추가
- 추가 후 입력창 비우기

FR-2 아이템 삭제:
- 🗑 버튼 클릭 즉시 삭제 (확인 대화상자 없음)
- 삭제된 id는 재사용하지 않음 (next_id는 계속 증가)
- 목록을 순회하면서 삭제하지 말고, 새 리스트를 만들어 교체할 것
- 하단에 "완료 항목 모두 삭제" 버튼 (완료 항목이 0개면 비활성화)

FR-3 아이템 수정:
- ✏️ 클릭 → session_state.editing_id에 해당 id 저장 → 그 행만 편집 모드로 렌더
- 편집 모드 행: 텍스트 입력창 + [저장] + [취소]
- 편집은 한 번에 한 항목만 (editing_id가 단일 값이므로 자연히 보장됨)
- 저장 시 validate_name(name, exclude_id=해당 id)로 검증. 자기 자신은 중복 검사 제외
- 취소 시 원래 값 유지하고 editing_id = None
- 수정 후에도 done 상태와 목록 순서가 그대로일 것

FR-4 체크:
- 체크박스 on_change 콜백 또는 반환값 비교로 done 토글
- 완료 항목은 이름에 취소선 (~~텍스트~~), 흐린 색
- 완료해도 목록 순서가 바뀌지 않을 것

FR-5 조회/보조:
- 상단에 "완료 N / 전체 M" 텍스트 + st.progress 진행률 바
  (전체가 0이면 0으로 나누지 않게 처리)
- 필터: st.radio로 전체 / 미완료 / 완료
- 목록이 비면: "아직 담은 항목이 없습니다. 위에서 추가해 보세요."
- 필터 결과만 비면: "해당 조건의 항목이 없습니다."

주의:
- 위젯 key는 계속 id 기반으로 유지
- 상태 변경 뒤 st.rerun() 호출 시, 콜백 안에서는 rerun이 필요 없다는 점 유의
- 예외로 앱이 죽는 경로가 없어야 함
```

**확인 사항:** PRD 10장의 AC-2 ~ AC-12를 직접 클릭해서 하나씩 통과시켜 본다.

---

## 3단계: UI 디자인

> **목표:** 기능은 그대로 두고 보기 좋게 만든다. 동작이 바뀌면 안 된다.

```
app.py의 기능은 그대로 두고 UI만 다듬어줘. 동작이나 검증 로직은 건드리지 마.

레이아웃 (PRD 7장 와이어프레임 기준):
- 아이템 행 컬럼 비율 조정: 체크박스는 좁게, 이름은 넓게, 버튼 2개는 좁게
  (예: [0.5, 6, 1, 1]) — 화면 폭이 좁아져도 버튼이 줄바꿈되지 않게
- 입력 폼도 [입력창, 추가버튼] 비율을 잡아 한 줄에 붙일 것
- 섹션 사이에 st.divider()로 구분

시각적 처리:
- 완료 항목: 취소선 + 회색 톤. 미완료 항목과 한눈에 구분될 것
- 삭제 버튼은 위험한 동작이므로 type="secondary", 추가 버튼은 type="primary"
- 진행률 바 위에 "완료 3 / 7 (43%)" 형태로 숫자와 퍼센트를 같이 표시
- 목록이 비었을 때는 st.info로 안내 (경고처럼 보이지 않게)

접근성 (PRD 9장):
- 아이콘만 있는 버튼에는 help 파라미터로 툴팁을 달 것
  ✏️ → help="이름 수정", 🗑 → help="삭제"
- 버튼 클릭 영역이 충분하도록 use_container_width 활용

제약:
- 외부 CSS 프레임워크나 커스텀 폰트를 끌어오지 말 것. Streamlit 기본 컴포넌트로 해결
- st.markdown(unsafe_allow_html=True)는 취소선/색상 정도에만 최소한으로 사용
- 라이트/다크 테마 양쪽에서 글자가 읽히는 색을 쓸 것 (하드코딩한 흰색/검정 배경 금지)
- app.py는 계속 단일 파일, 200줄 내외 유지

수정 전후로 동작이 달라진 게 없는지 스스로 확인하고 알려줘.
```

**확인 사항:** 2단계에서 통과했던 AC들이 여전히 통과하는가 / 다크 테마에서도 글자가 읽히는가.

---

## 4단계: PRD 대조 검토 및 버그 점검

> **목표:** 구현이 PRD와 일치하는지, 숨은 버그가 없는지 항목별로 판정한다.

```
app.py와 requirements.txt를 PRD.md와 대조해서 자세히 검토해줘.
코드를 고치기 전에 먼저 검토 결과부터 보고할 것.

A. PRD 요구사항 대조
- FR-1 ~ FR-5의 모든 항목을 하나씩 짚어서 [구현됨 / 부분 구현 / 누락]으로 판정
- PRD 10장 AC-1 ~ AC-12를 코드 기준으로 통과 여부 판정하고, 통과 못 하면 이유를 코드 위치(app.py:줄번호)와 함께 제시
- PRD 6장 데이터 모델과 실제 session_state 구조가 일치하는지
- PRD 3.2의 비목표에 해당하는 기능이 슬쩍 들어가 있지 않은지

B. Streamlit 특유의 버그 점검 (아래를 하나씩 실제로 확인)
1. 위젯 key 충돌: 같은 key가 두 번 생성되는 경로가 있는가
2. key에 리스트 인덱스를 쓴 곳이 남아 있는가 (삭제 시 체크 상태가 다른 항목으로 밀림)
3. 삭제된 아이템의 위젯 key가 session_state에 남아 다음 렌더에 영향을 주는가
4. 리스트를 순회하는 도중에 원소를 삭제하는 코드가 있는가
5. st.rerun() 위치: 콜백 안에서 불필요하게 호출하거나, 필요한 곳에서 빠졌는가
6. st.form 안에서 form_submit_button 외의 버튼으로 상태를 바꾸려 하는가
7. init_state()가 기존 상태를 덮어써서 값이 초기화되는 경로가 있는가
8. editing_id가 가리키는 아이템이 삭제됐을 때 어떻게 되는가 (편집 모드에서 멈추거나 예외)
9. 필터가 걸린 상태에서 수정/삭제/체크를 했을 때 엉뚱한 항목에 적용되는가
10. 아이템이 0개일 때 진행률 계산에서 0으로 나누는가

C. 입력 검증 경계 확인
- 공백만 입력 / 앞뒤 공백이 섞인 입력 / 정확히 50자 / 51자
- 대소문자만 다른 중복 ("우유" vs "우유 ", "Milk" vs "milk")
- 수정 시 자기 이름 그대로 저장 (자기 자신을 중복으로 판정하면 버그)
- 이모지나 특수문자가 포함된 이름

D. 코드 품질
- PRD 8.1에 명시된 함수들이 실제로 분리돼 있는가, 아니면 한 덩어리인가
- 중복된 로직이 있는가
- app.py 줄 수 (목표 200줄 내외)

보고 형식:
- 발견한 문제를 심각도 순으로 정렬. 각 항목마다
  [파일:줄번호] / 무엇이 잘못됐는지 / 어떤 조작을 하면 재현되는지 / 어떻게 고칠지
- 문제가 없는 항목은 "이상 없음"으로 짧게만 표시하고 길게 설명하지 말 것
- 추측이 아니라 실제 코드를 읽고 판정할 것. 확신이 없으면 "확인 필요"로 표시

보고를 받고 내가 고칠 항목을 고르면 그때 수정해줘.
```

**확인 사항:** 보고서의 각 지적을 앱에서 직접 재현해 본 뒤 수정 여부를 결정한다.

---

## 부록: 추가로 쓸 만한 프롬프트

### 수정 적용
```
4단계 검토 보고서에서 [심각도 높음] 항목만 수정해줘.
수정할 때마다 어떤 AC가 다시 통과하게 되는지 밝히고,
수정으로 인해 깨질 수 있는 다른 기능이 있으면 먼저 알려줘.
```

### README 작성
```
PRD.md를 토대로 README.md를 작성해줘.
설치, 실행, 기능 소개, 그리고 "새로고침하면 목록이 사라진다"는 제약을 명시할 것.
```

### 영속성 추가 (PRD 11장 확장 1번)
```
PRD.md 11장의 확장 1번(JSON 파일 저장)을 구현해줘.
- 프로젝트 폴더의 items.json에 저장/로드
- 앱 시작 시 파일이 없으면 빈 목록으로 시작
- 파일이 깨져 있으면 앱이 죽지 않고 빈 목록으로 시작 + 경고 표시
- 기존 함수 시그니처는 유지하고, 저장 시점만 추가할 것
```
