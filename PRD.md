# 쇼핑 리스트 앱 PRD

| 항목 | 내용 |
|---|---|
| 문서 버전 | 1.1 |
| 작성일 | 2026-09-08 |
| 상태 | 구현 완료 |
| 변경 이력 | 1.1 — `items.json` 파일 영속성 추가 (3.2 / 6 / 11장) |

---

## 1. 개요

장보기 목록을 화면 하나에서 관리하는 웹 앱. 아이템을 추가하고, 이름/수량을 고치고, 구매 완료를 체크하고, 필요 없어진 항목을 지운다. 로그인·회원가입·서버 DB 없이 브라우저 세션 안에서 동작하는 단일 페이지 앱으로 만든다.

**한 줄 정의:** Streamlit으로 만든 단일 사용자용 쇼핑 리스트 CRUD 앱.

## 2. 배경 및 목적

- 종이 메모나 메신저 "나에게 보내기"로 장보기 목록을 관리하면 항목 수정·완료 표시가 번거롭다.
- 기존 투두 앱들은 프로젝트/태그/알림 등 장보기에 필요 없는 기능이 많다.
- 목적: 추가·삭제·수정·체크 네 가지 동작만 남긴, 클릭 두 번 안에 끝나는 최소 기능 앱을 만든다.

## 3. 목표 / 비목표

### 3.1 목표
1. 아이템 추가·삭제·수정·체크(완료 토글) 네 가지 기능이 모두 동작한다.
2. `streamlit run app.py` 한 줄로 실행된다.
3. 의존성이 `requirements.txt` 하나로 재현 가능하다.
4. 코드가 단일 파일(`app.py`) 안에서 읽히는 분량으로 유지된다.

### 3.2 비목표 (이번 범위에서 제외)
- 사용자 인증, 다중 사용자, 목록 공유
- 데이터베이스 연동, 서버 배포
- 카테고리, 가격 합계, 마감일, 알림, 이미지 첨부
- 모바일 전용 UI, 다국어

## 4. 사용자 및 사용 시나리오

**대상 사용자:** 장보기 전 살 것을 정리하고, 마트에서 하나씩 체크해 나가는 개인 사용자.

| # | 시나리오 | 흐름 |
|---|---|---|
| S1 | 목록 작성 | 입력창에 "우유" 입력 → 추가 → 목록에 미완료 상태로 나타남 |
| S2 | 수량 정정 | "계란"을 "계란 2판"으로 고쳐야 함 → 수정 버튼 → 인라인 입력창에서 변경 → 저장 |
| S3 | 매장에서 체크 | 카트에 담을 때마다 체크박스 클릭 → 완료 표시(취소선) |
| S4 | 항목 취소 | 안 사기로 한 항목 → 삭제 버튼 → 목록에서 즉시 제거 |
| S5 | 진행 확인 | 화면 상단에서 "3 / 7 완료" 확인 |

## 5. 기능 요구사항

### FR-1. 아이템 추가
- 텍스트 입력창 + "추가" 버튼을 화면 상단에 배치한다.
- Enter 키로도 추가된다.
- 추가 시 새 아이템은 목록 **맨 아래**에 미완료 상태로 들어간다.
- 각 아이템은 고유 ID를 갖는다. (수정·삭제의 식별자로 사용)
- **검증:**
  - 공백만 입력 → 추가하지 않고 경고 메시지 표시.
  - 앞뒤 공백은 제거(trim) 후 저장.
  - 이미 있는 이름과 중복 → 경고 메시지 표시 후 추가하지 않음. (대소문자·공백 무시하고 비교)
  - 최대 길이 50자. 초과 시 경고.
- 추가 후 입력창은 비워진다.

### FR-2. 아이템 삭제
- 각 행에 삭제 버튼(🗑)을 둔다.
- 클릭 즉시 삭제하고 목록을 다시 그린다. (확인 대화상자 없음 — 재입력 비용이 낮으므로)
- 삭제된 아이템의 ID는 재사용하지 않는다.
- 추가 요구: "완료 항목 모두 삭제" 버튼을 하나 둔다.

### FR-3. 아이템 수정
- 각 행에 수정 버튼(✏️)을 둔다.
- 클릭 시 해당 행이 편집 모드로 바뀐다: 이름이 텍스트 입력창으로 바뀌고 "저장" / "취소" 버튼이 나타난다.
- 편집 모드는 한 번에 한 항목만 활성화된다.
- 저장 시 FR-1과 동일한 검증 규칙을 적용한다. (단, 중복 검사에서 자기 자신은 제외)
- 취소 시 원래 값을 유지하고 편집 모드를 빠져나온다.
- 수정해도 완료 상태와 목록 순서는 유지된다.

### FR-4. 체크(완료 토글)
- 각 행 맨 앞에 체크박스를 둔다.
- 체크 시 완료 상태가 되고, 이름에 취소선 + 흐린 색을 적용한다.
- 체크 해제 시 미완료로 되돌아간다.
- 완료 여부가 목록 순서를 바꾸지 않는다. (정렬 옵션은 FR-5)

### FR-5. 목록 조회 및 보조 기능
- 상단에 진행 상황 표시: `완료 N / 전체 M`, 진행률 바.
- 필터: `전체` / `미완료` / `완료` 라디오 버튼.
- 목록이 비었을 때 안내 문구 표시 ("아직 담은 항목이 없습니다. 위에서 추가해 보세요.").

## 6. 데이터 모델

세션 상태에 아이템 리스트 하나를 유지한다.

```python
# st.session_state["items"]: list[dict]
{
    "id":   int,   # 고유 식별자, 단조 증가
    "name": str,   # 아이템 이름, 1~50자, trim 완료
    "done": bool,  # 체크 여부
}
```

보조 세션 상태:

| 키 | 타입 | 용도 |
|---|---|---|
| `items` | `list[dict]` | 아이템 목록 (위 스키마) |
| `next_id` | `int` | 다음 아이템에 부여할 ID |
| `editing_id` | `int \| None` | 현재 편집 모드인 아이템 ID, 없으면 `None` |

**영속성:** `app.py`와 같은 폴더의 `items.json`에 저장한다. (v1.1에서 추가 — 원래는 세션 한정이었으나 새로고침 시 목록이 사라지는 문제로 범위에 포함)

```json
{ "next_id": 3, "items": [{ "id": 1, "name": "우유", "done": false }] }
```

- 아이템이 바뀔 때마다(추가·삭제·수정·체크) 즉시 저장한다.
- 임시 파일에 쓴 뒤 교체해서, 쓰는 도중 중단돼도 기존 파일이 깨지지 않는다.
- 앱 시작 시 파일이 없으면 빈 목록으로 시작한다.
- 파일이 깨져 있으면 앱이 죽지 않고 빈 목록 + 경고를 띄운다.
- 읽기는 `utf-8-sig`로 한다. 메모장이 붙인 BOM 때문에 목록을 통째로 잃지 않도록.
- `next_id`는 저장값과 `max(id)+1` 중 큰 값을 쓴다. 파일을 손으로 고쳐도 id가 겹치지 않는다.

## 7. 화면 설계

```
┌────────────────────────────────────────────┐
│  🛒 쇼핑 리스트                              │
├────────────────────────────────────────────┤
│  [ 아이템 입력...            ]  [ 추가 ]    │
├────────────────────────────────────────────┤
│  완료 2 / 5                                 │
│  ████████░░░░░░░░░░░░  40%                  │
│  ( ) 전체   ( ) 미완료   ( ) 완료            │
├────────────────────────────────────────────┤
│  [x]  ~~우유~~              [✏️] [🗑]       │
│  [ ]  계란 2판              [✏️] [🗑]       │
│  [ ]  [ 사과___ ] [저장] [취소]   ← 편집 중  │
│  [x]  ~~식빵~~              [✏️] [🗑]       │
├────────────────────────────────────────────┤
│  [ 완료 항목 모두 삭제 ]                     │
└────────────────────────────────────────────┘
```

- 레이아웃: `st.columns`로 한 행을 `[체크박스 | 이름 | 수정 | 삭제]` 4열로 구성한다.
- 완료 항목의 취소선은 `st.markdown`의 `~~텍스트~~` 또는 인라인 HTML로 처리한다.
- 상태 변경 후에는 `st.rerun()`으로 화면을 다시 그린다.

## 8. 기술 요구사항

| 항목 | 내용 |
|---|---|
| 언어 | Python 3.9 이상 |
| 프레임워크 | Streamlit |
| 실행 | `streamlit run app.py` |
| 외부 API | 없음 |
| 파일 구조 | `app.py`, `requirements.txt` (선택: `README.md`) |

### 8.1 산출물

**`app.py`**
- 단일 파일. 상태 초기화 → 헤더 → 입력 폼 → 요약/필터 → 목록 렌더 → 하단 액션 순으로 위에서 아래로 읽히게 구성한다.
- 동작 단위를 함수로 분리한다: `init_state()`, `add_item(name)`, `delete_item(item_id)`, `update_item(item_id, name)`, `toggle_item(item_id)`, `validate_name(name, exclude_id)`.
- 위젯 `key`는 아이템 ID를 포함해 고유하게 만든다 (예: `f"chk_{item['id']}"`). 인덱스 기반 key는 삭제 시 상태가 어긋나므로 쓰지 않는다.

**`requirements.txt`**
- `streamlit` 한 줄. 버전은 하한을 명시한다 (예: `streamlit>=1.30`).

## 9. 비기능 요구사항

- **성능:** 아이템 100개까지 조작 시 체감 지연 없이 리렌더된다.
- **안정성:** 잘못된 입력으로 앱이 예외를 던지지 않는다. 모든 사용자 입력은 검증 후 반영한다.
- **가독성:** `app.py`는 200줄 내외를 목표로 한다.
- **접근성:** 버튼에 아이콘만 쓰지 않고 `help` 툴팁으로 동작을 설명한다.

## 10. 완료 기준 (Acceptance Criteria)

| # | 기준 | 통과 조건 |
|---|---|---|
| AC-1 | 실행 | 새 가상환경에서 `pip install -r requirements.txt` → `streamlit run app.py` 로 앱이 뜬다 |
| AC-2 | 추가 | "우유" 입력 후 Enter → 목록에 "우유"가 미완료로 나타난다 |
| AC-3 | 빈 입력 | 공백만 입력 후 추가 → 항목이 늘지 않고 경고가 보인다 |
| AC-4 | 중복 | 이미 있는 "우유"를 다시 추가 → 항목이 늘지 않고 경고가 보인다 |
| AC-5 | 수정 | ✏️ → "우유 2팩"으로 저장 → 이름만 바뀌고 순서·완료 상태는 그대로다 |
| AC-6 | 수정 취소 | ✏️ → 값 변경 → 취소 → 원래 이름이 유지된다 |
| AC-7 | 체크 | 체크박스 클릭 → 취소선이 적용되고 완료 수가 1 늘어난다 |
| AC-8 | 체크 해제 | 다시 클릭 → 취소선이 사라지고 완료 수가 1 줄어든다 |
| AC-9 | 삭제 | 🗑 클릭 → 해당 항목만 사라지고 나머지 항목의 상태가 유지된다 |
| AC-10 | 필터 | "미완료" 선택 → 완료 항목이 목록에서 감춰진다 |
| AC-11 | 빈 목록 | 항목을 모두 지우면 안내 문구가 보인다 |
| AC-12 | 일괄 삭제 | "완료 항목 모두 삭제" → 완료 항목만 사라진다 |

## 11. 향후 확장 (이번 범위 밖)

1. ~~JSON 파일 저장으로 새로고침 후에도 목록 유지~~ → v1.1에서 구현 (6장 참조)
2. 수량·단위 필드 분리 (`계란 / 2 / 판`)
3. 카테고리(신선식품, 생필품 등)별 그룹핑
4. 목록 텍스트 내보내기 / 붙여넣기로 일괄 추가
5. 드래그 순서 변경
