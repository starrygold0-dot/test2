# Python
__pycache__/
*.py[cod]
.venv/
venv/

# 앱 데이터 — 개인 장보기 목록이므로 저장소에 올리지 않는다.
# 앱은 파일이 없으면 빈 목록으로 시작한다.
items.json
items.json.tmp

# Streamlit 로컬 설정 (secrets.toml에 자격 증명이 들어갈 수 있다)
.streamlit/secrets.toml

# 도구가 남긴 파일
.playwright-mcp/
